source "https://rubygems.org"

# Use the same GitHub Pages-compatible dependency set as the original site.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end


